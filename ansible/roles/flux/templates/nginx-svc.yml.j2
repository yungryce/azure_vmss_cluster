---
apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: default  # Static value or use {{ namespace }} if needed
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

